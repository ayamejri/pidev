{% extends 'base.html.twig' %}

{% block title %}New Thread{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Create New Thread</h1>

        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        {{ include('thread/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>

        <a href="{{ path('app_thread_indexf') }}" class="btn btn-secondary mt-3">Back to List</a>
    </div>
{% endblock %}
